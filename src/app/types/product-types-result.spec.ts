import { ProductTypesResult } from './product-types-result';

describe('ProductTypesResult', () => {
  it('should create an instance', () => {
    expect(new ProductTypesResult()).toBeTruthy();
  });
});
